import { Meta } from "@storybook/blocks";

import { ReconnectingEvidence } from "./VersionIndicatorEvidence";

<Meta title="Components/VersionIndicator/Reconnecting evidence" />

<style>{`body { background: #101820 !important; }`}</style>

<div
  style={{
    background: "#101820",
    color: "#e7edf3",
    padding: "48px",
  }}
>
  <h1 style={{ color: "#e7edf3" }}>Reconnecting</h1>
  <p>
    The node is restarting after the upgrade request, so the popover remains
    open while the client observes status recovery.
  </p>
  <ReconnectingEvidence />
</div>
