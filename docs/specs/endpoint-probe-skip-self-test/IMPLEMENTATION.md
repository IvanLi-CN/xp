# Implementation

## Current State

- `XP_ENDPOINT_PROBE_SKIP_SELF_TEST` and the matching CLI option are implemented.
- A hosting node reports skipped self-tests without attempting the public ingress path.
- `docs/ops/endpoint-probe.md` defines the operator-facing escape-hatch semantics.
