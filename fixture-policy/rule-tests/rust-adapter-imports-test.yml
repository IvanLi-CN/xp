id: fixture-policy-rust-adapter-imports
valid:
  - |
    use std::sync::OnceLock;
  - |
    use serde::Deserialize;
  - |
    use super::catalog;
  - |
    pub use operations::*;
invalid:
  - |
    use std::fs;
