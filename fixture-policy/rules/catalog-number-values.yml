id: fixture-policy-catalog-number-values
language: Json
severity: error
message: Fixture catalog numbers must be explicitly approved synthetic values.
files:
  - fixture-policy/catalog.json
rule:
  all:
    - kind: number
    - not:
        regex: '^(0|[1-9][0-9]{0,5}|0\.[0-9]{1,3})$'
