id: fixture-policy-typescript-data-source
language: TypeScript
severity: error
message: Fixture operational data must be a direct fixtureCatalog accessor call.
note: Add a safe synthetic value to fixture-policy/catalog.json and expose it through the adapter.
files:
  - web/src/**/*.test.ts
  - web/src/**/*.spec.ts
  - web/tests/**/*.ts
  - web/src/**/*.stories.ts
  - web/.storybook/mocks/**/*.ts
  - web/src/storybook/**/*.ts
  - web/src/demo/fixtures.ts
rule:
  all:
    - kind: pair
    - has:
        field: key
        any:
          - regex: '^(accessHost|access_host|apiBaseUrl|api_base_url|host|hostname|serverNames|server_names|acceptedAuthorities|accepted_authorities|canaryUpstreamUrl|canary_upstream_url|canary_upstream|nodeId|node_id|nodeName|node_name|endpointId|endpoint_id|endpointTag|endpoint_tag|subscriptionToken|subscription_token|token|secret|password|ip|public_ipv4|selected_public_ip|dest|destination|xray_api_addr|tag|bind|cluster_id|leader_api_base_url|mesh_url|meshBaseUrl|mesh_base_url|publicBaseUrl|public_base_url|publicOrigin|runId|run_id|configHash|config_hash)$'
          - regex: '^(lastSeenAt|last_seen_at|lastProbeAt|last_probe_at|lastSyncedAt|last_synced_at|lastOkAt|last_ok_at|lastFailAt|last_fail_at|downSince|down_since|lastRestartAt|last_restart_at|lastRestartFailAt|last_restart_fail_at|slotStart|slot_start|observedAt|observed_at|occurredAt|occurred_at|windowStart|window_start|windowEnd|window_end|minute|startMinute|start_minute|endMinute|end_minute|createdAt|created_at|updatedAt|updated_at|checkedAt|checked_at|connectedAt|connected_at|generatedAt|generated_at|lastSampleAt|last_sample_at|lastTransitionAt|last_transition_at|lastConnectionStartedAt|last_connection_started_at|cycleStartAt|cycle_start_at|cycleEndAt|cycle_end_at|trackingSince|tracking_since|latestCheckedAt|latest_checked_at|lastSuccessAt|last_success_at|hour)$'
          - regex: '^(latencyMs|latency_ms|probeLatencyMs|probe_latency_ms|latency_p50_ms|latency_p95_ms|availability_1h|availability_24h|mesh_availability_24h|connection_generation|current_connection_requests|requests_5m|connection_starts_5m|requests_1h|connection_starts_1h|trafficBytes|traffic_bytes|uplink_bytes|downlink_bytes|totalBytes|total_bytes|requests|connections|throughput)$'
          - regex: '^(startedAt|started_at|finishedAt|finished_at)$'
          - regex: '^(completedAt|completed_at|emittedAt|emitted_at)$'
          - regex: '^(startAt|start_at|endAt|end_at)$'
          - regex: '^(windowStartAt|window_start_at)$'
          - regex: '^(windowEndAt|window_end_at|cycleStartAt|cycle_start_at)$'
          - regex: '^(cycleEndAt|cycle_end_at|trackingSince|tracking_since)$'
    - has:
        field: value
        not:
          any:
            - pattern: fixtureCatalog.$GROUP.$ACCESSOR()
            - pattern: 'null'
    - not:
        all:
          - has:
              field: value
              any:
                - pattern: endpoint.endpoint_id
                - pattern: endpoint.node_id
          - inside:
              stopBy: end
              pattern: |
                ($ENDPOINT_ID) => {
                  const endpoint = endpointById.get($ENDPOINT_ID);
                  if (!endpoint) throw new Error($ERROR);
                  return { $$$ };
                }
    - not:
        all:
          - has:
              field: value
              any:
                - regex: '^`\$\{derived\[0\]\}:443`$'
                - pattern: derived
                - pattern: '[$NODE.access_host.replace($$$)]'
                - pattern: 'payload.canary_upstream ? { $$$ } : undefined'
                - pattern: acceptedAuthorities
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: buildEndpointCreateMeta
    - not:
        all:
          - has:
              field: value
              any:
                - pattern: node.node_id
                - pattern: node.node_name
                - pattern: node.api_base_url
                - pattern: node.access_host
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    regex: '^(buildNodeRuntimeListItem|buildNodeHistory|buildDefaultUserTraffic)$'
    - not:
        all:
          - has:
              field: value
              any:
                - regex: '^`\$\{derived\[0\]\}:443`$'
                - pattern: derived
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: refreshGlobalEndpointReality
    - not:
        all:
          - has:
              field: value
              any:
                - pattern: endpoint.endpoint_id
                - pattern: endpoint.node_id
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: applyAutoAssignForEndpoint
    - not:
        all:
          - has:
              field: value
              pattern: payload.node_name ?? node.node_name
          - has:
              field: key
              regex: '^node_name$'
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: handleRequest
    - not:
        all:
          - has:
              field: value
              any:
                - pattern: payload.access_host ?? node.access_host
                - pattern: node.access_host
          - has:
              field: key
              regex: '^access_host$'
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: handleRequest
    - not:
        all:
          - has:
              field: value
              any:
                - pattern: payload.api_base_url ?? node.api_base_url
                - pattern: node.api_base_url
          - has:
              field: key
              regex: '^api_base_url$'
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: handleRequest
    - not:
        all:
          - has:
              field: value
              any:
                - pattern: endpointId
                - pattern: payload.node_id
                - pattern: nodeId
                - pattern: item.node_id
                - pattern: q.node_id
                - pattern: node.node_id
          - has:
              field: key
              regex: '^node_id$'
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: handleRequest
    - not:
        all:
          - has:
              field: value
              any:
                - pattern: endpointId
                - pattern: endpoint.endpoint_id
          - has:
              field: key
              regex: '^endpoint_id$'
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: handleRequest
    - not:
        all:
          - has:
              field: value
              any:
                - pattern: tag
                - pattern: endpoint.tag
          - has:
              field: key
              regex: '^(tag|endpoint_tag)$'
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: handleRequest
    - not:
        all:
          - has:
              field: value
              pattern: user.subscription_token
          - has:
              field: key
              regex: '^subscription_token$'
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    any:
                      - pattern: handleRequest
                      - pattern: setupApiMocks
    - not:
        all:
          - has:
              field: value
              pattern: nextSubscriptionToken()
          - has:
              field: key
              regex: '^subscription_token$'
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    any:
                      - pattern: handleRequest
                      - pattern: setupApiMocks
    - not:
        all:
          - has:
              field: value
              pattern: run.run_id
          - has:
              field: key
              regex: '^run_id$'
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: handleEndpointProbeRequest
    - not:
        all:
          - has:
              field: value
              pattern: nodeId
          - has:
              field: key
              regex: '^node_id$'
          - inside:
              stopBy: end
              all:
                - kind: function_declaration
                - has:
                    field: name
                    pattern: setupApiMocks
